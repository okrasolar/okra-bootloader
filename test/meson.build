if get_option('GIGADEVICE').enabled()
    test_files = files([
        'main.cpp',
        'bootlogictest_gd.cpp'
    ])
else
    test_files = files([
        'main.cpp',
        'bootlogictest_stm.cpp'
    ])
endif
